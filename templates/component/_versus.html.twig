{% block content %}
	<div class="versus">
		<div class="user-image">
			<img src="{{ asset('build/images/' ~ app.user.avatar) }}" alt="Avatar" class="userPic">
		</div>
		<div class="versus-image">
			<img src="{{ asset('build/images/versus.png')}}" alt="Versus" class="userPic">
		</div>
		<div
			class="opponent-image">
			<!-- the image src is destined to be changed when the opponent avatar is accessible -->
			<img src="{{ asset('build/images/' ~ app.session.get('opponent').avatar) }}" alt="Avatar" class="userPic">
		</div>
	</div>
{% endblock %}
